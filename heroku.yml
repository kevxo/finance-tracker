build:
  docker:
    backend: ./backend/Dockerfile
    frontend: ./frontend/finance-tracker/Dockerfile  # Frontend image from Docker Hub

release:
  command:
    - cd backend && alembic upgrade head  # Run migrations on the backend service

run:
  web:
    command:
      - docker compose up
