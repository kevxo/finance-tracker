setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE # Automatically provisions a PostgreSQL database

build:
  docker:
    backend: ./backend/Dockerfile
    frontend: ./frontend/finance-tracker/Dockerfile

release:
  command:
    - backend: cd backend && alembic upgrade head # Run migrations for backend

run:
  web: docker compose up
