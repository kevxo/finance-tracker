---
  build:
    docker:
      web:
        dockerfile: ./backend/Dockerfile
      frontend:
        dockerfile: ./frontend/finance-tracker/Dockerfile
  run:
    web:
      command:
        - docker compose up
